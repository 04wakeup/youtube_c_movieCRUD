extends layouts/main
include mixins/movieBlock

block content
    .title
        h1 #{pageTitle}
    .movies 
        each movie in movies 
            +movieBlock({
                id: movie.id,
                title: movie.title,
                year: movie.year,
                rating: movie.rating
            })